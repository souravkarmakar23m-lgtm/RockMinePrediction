# RockMinePrediction
This project is a binary classification model that predicts whether an object is a Rock (R) or a Mine (M) using sonar signal data. The model is built using Logistic Regression and trained on a dataset containing sonar frequency measurements.
